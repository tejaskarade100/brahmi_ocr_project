# Model, Dataset, And Training README

This document explains the OCR model, how the dataset is organized, how synthetic data is generated, how preprocessing and prediction work, and how to interpret the training metrics you shared.

## Model Choice: What TrOCR Is

TrOCR is a Transformer-based OCR model from Microsoft. In Hugging Face it is exposed through:

- `TrOCRProcessor`
- `VisionEncoderDecoderModel`

The idea is:

- an image encoder reads the text image
- a text decoder generates the output sequence token by token

This project uses:

- base checkpoint: `microsoft/trocr-small-printed`

Why that choice makes sense here:

- it is much stronger than older character-by-character CNN pipelines
- it predicts full sequences, not only isolated labels
- it is a good fit for words, phrases, and line-level OCR
- it can be adapted to Brahmi by expanding the tokenizer with Brahmi characters

## How The Brahmi Model Is Adapted

During training in [`train.py`](train.py):

1. The processor and model are loaded from the TrOCR checkpoint.
2. The dataset character inventory is collected from all labels.
3. Any missing Brahmi characters are added to the tokenizer.
4. The decoder token embeddings are resized.
5. The generation config is updated for OCR decoding.

From your run:

- 109 tokens were added to the tokenizer
- the vocabulary size grew to `64111`

This is the key adaptation step that makes TrOCR usable for Brahmi Unicode output.

## Why The Missing Pooler Weights Warning Is Acceptable

Your log shows:

- `encoder.pooler.dense.bias | MISSING`
- `encoder.pooler.dense.weight | MISSING`

That looks scary at first, but in this project it is acceptable because:

- TrOCR for OCR generation mainly relies on the encoder features and decoder generation path
- the classification-style pooler is not the main component for seq2seq OCR
- Hugging Face initializes those missing weights automatically

So this warning should be documented, not treated as a failure.

## Dataset Design

The project is built for a hybrid dataset:

- structured fixed classes from folder labels
- generated synthetic images for coverage and balance
- mixed text for words, phrases, sentences, and multiline text

In this repository snapshot, the packaged image set is mostly generated, but the data format and loaders support mixing curated real samples with synthetic samples.

The dataset structure is controlled by [`dataset/map.json`](../dataset/map.json).

Main groups:

- `1Vowels`
- `2Consonants`
- `3Numbers`
- `4Extras`
- `5Words_Phrases`

Mapped entry breakdown:

| Group | Entries | Share |
| --- | ---: | ---: |
| Vowels | 14 | 2.35% |
| Consonant and matra combinations | 518 | 86.77% |
| Numbers | 30 | 5.03% |
| Extras and signs | 34 | 5.70% |
| Mixed text bucket | 1 | 0.17% |

The important point is that the dataset is heavily oriented toward real Brahmi combinations, not only isolated base glyphs.

## Hybrid Curated + Generated Dataset

The project should be described as a hybrid curated-plus-synthetic OCR dataset design, not as a purely manual dataset.

Accurate wording for documentation:

> We used a hybrid dataset made of curated folder-labeled Brahmi classes and large-scale synthetic generation for balanced OCR training across characters, words, phrases, sentences, and multiline text.

That wording matches the repository.

## How Synthetic Data Is Generated

Synthetic generation lives in [`dataset/generate_synthetic.py`](../dataset/generate_synthetic.py).

Generation process:

1. Read the manifest generated by [`build_targets.py`](../dataset/build_targets.py).
2. For fixed classes, render the mapped Brahmi label directly.
3. For mixed text, create a synthetic sequence based on `word`, `phrase`, `sentence`, or `multiline`.
4. Render the text with `NotoSansBrahmi-Regular.ttf`.
5. Apply one of three style pipelines.
6. Save the image and, for mixed text, append metadata to `labels.json`.

## Background And Style Percentages

The generator explicitly uses this style split:

- clean background style: 40%
- manuscript-like background style: 35%
- stone-like background style: 25%

This is not guessed from the data. It is hardcoded in the manifest-building logic and reflected in the generated filenames and labels metadata.

If you want a documentation sentence, use:

> Generated backgrounds were distributed as 40% clean, 35% manuscript-like, and 25% stone-like to expose the OCR model to both controlled and degraded visual conditions.

## Sequence-Type Percentages

For mixed text under `5Words_Phrases`, the current dataset metadata is:

| Sequence type | Count | Share |
| --- | ---: | ---: |
| Word | 10,000 | 35.71% |
| Phrase | 10,000 | 35.71% |
| Sentence | 4,000 | 14.29% |
| Multiline | 4,000 | 14.29% |

Across the full generated image set, fixed classes dominate because there are many mapped character folders. That is why runtime capping and balanced sampling are important during training.

## Vowel Breakdown: How To Explain It Correctly

There are two different "vowel percentages" in this project, and they should not be mixed up.

### 1. Inventory-level vowel share

Independent vowels are 14 of 597 mapped entries, or 2.35% of the map inventory.

That is a class-inventory number, not a text-frequency number.

### 2. Mixed-text generation vowel bias

In generated words:

- an independent vowel can appear in the first position with 15% probability
- otherwise the generator prefers consonant syllables
- after the first position, consonant syllables are preferred about 85% of the time

This means the synthetic text is intentionally consonant-heavy, which is expected for Brahmi sequence generation and also helps keep generated text visually plausible.

So a careful README should say:

> Independent vowels are 2.35% of the mapped class inventory, while mixed-text generation uses a 15% initial independent-vowel chance and otherwise prefers consonant syllables to keep sequences realistic.

## Dataset Size On Disk

Current image counts in the repository dataset are:

| Folder | Images |
| --- | ---: |
| `1Vowels` | 1,400 |
| `2Consonants` | 51,800 |
| `3Numbers` | 3,000 |
| `4Extras` | 3,400 |
| `5Words_Phrases` | 28,000 |
| Total | 87,600 |

## Runtime Training Caps

Training does not necessarily consume every image on disk. [`dataset_loader.py`](dataset_loader.py) applies runtime caps such as:

- `max_fixed_per_class`
- `max_words`
- `max_phrases`
- `max_long_sentences`

This matters because the dataset on disk is much larger and more skewed than the balanced subset that is actually fed to the model during a training run.

## Training Configuration

The default training script includes:

- model: `microsoft/trocr-small-printed`
- optimizer: `AdamW`
- scheduler: cosine decay with warmup
- batch size: `2`
- gradient accumulation: `8`
- image size: `384`
- early stopping patience: `3`
- optional balanced sampling
- optional FP16 on GPU

Recommended training command from this repository:

```bash
python training/train.py --balanced_sampling --gradient_accumulation_steps 8 --image_size 384
```

## Training Breakdown From Your Logged Run

Your training log showed the following dataset split used for that run:

### Train

- total samples: `47,488`
- characters/n-grams: `25,172` (`53.01%`)
- words: `7,987` (`16.82%`)
- phrases: `7,952` (`16.75%`)
- long sentences: `6,377` (`13.43%`)

### Validation

- total samples: `5,936`
- characters/n-grams: `3,074` (`51.79%`)
- words: `998` (`16.81%`)
- phrases: `1,036` (`17.45%`)
- long sentences: `828` (`13.95%`)

These percentages are much healthier than feeding the model an overwhelmingly character-only stream.

## How To Explain Training Progress

From your log, the model improved steadily through the first six epochs:

| Epoch | Train Loss | Val Loss | CER | WER | Exact | First Char |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 6.5537 | 2.7321 | 1.0076 | 1.4880 | 0.03 | 0.34 |
| 2 | 2.6247 | 1.7909 | 0.6803 | 1.0484 | 0.09 | 0.81 |
| 3 | 1.7528 | 1.1721 | 0.4798 | 0.8860 | 0.31 | 0.89 |
| 4 | 1.1403 | 0.7874 | 0.3369 | 0.5923 | 0.66 | 0.92 |
| 5 | 0.9008 | 0.6825 | 0.3023 | 0.4785 | 0.73 | 0.95 |
| 6 | 0.6941 | 0.4422 | 0.1969 | 0.4051 | 0.77 | 0.96 |

This shows:

- loss is decreasing
- CER and WER are improving
- exact full-string matches are increasing
- the model is learning sequence length better because the length ratio approaches `1.03`

## How To Present Accuracy Without Overclaiming

You asked for documentation-friendly accuracy wording rather than a hard benchmark claim. The safest phrasing is to call them indicative or project-reported validation figures.

Use wording like:

> In the documented validation run, the model reached about 80.3% character-level accuracy proxy when interpreted as `1 - CER`, around 77% exact-sequence match, and about 96% first-character accuracy by epoch 6. These are project-run indicators rather than a formal benchmark.

That is faithful to your log without pretending it is a publication-grade metric.

## Category-Wise Validation Interpretation

Epoch 6 category metrics from your log:

- char: CER `0.0448`, WER `0.0384`
- word: CER `0.0139`, WER `0.0631`
- phrase: CER `0.0415`, WER `0.1982`
- long: CER `0.3091`, WER `0.7525`

Interpretation:

- isolated chars and short words are already quite strong
- phrases are decent but still noisier than single words
- long sequences remain the hardest part of the problem

That is exactly what you would expect in an OCR system that has recently been rebalanced away from character-only dominance.

## Preprocessing During Inference

[`utils/preprocess.py`](../utils/preprocess.py) applies:

1. grayscale conversion
2. Gaussian denoising
3. CLAHE contrast enhancement
4. optional thresholding
5. aspect-ratio-preserving resize with padding

Why this matters:

- ancient inscriptions often have uneven lighting and stone texture
- the OCR model wants a stable, normalized input image
- letterboxing avoids squashing glyph shapes into a square

Thresholding modes:

- `adaptive`
- `otsu`
- `simple`
- `auto`

The `auto` mode decides whether heavy thresholding is needed based on contrast and blur heuristics.

## How Prediction Works

[`predict.py`](../inference/predict.py) performs:

1. model loading
2. optional preprocessing
3. optional line segmentation
4. square letterboxing for each crop
5. beam-search decoding
6. optional token and character trace extraction

Generation settings used for decoding include:

- beam size: `4`
- `no_repeat_ngram_size=3`
- `length_penalty=2.0`
- `early_stopping=True`

These settings reduce collapse and encourage more stable sequence generation.

## Multiline Prediction

For multiline OCR, the script:

- thresholds the image
- computes row-wise activity
- finds active line bands
- crops each line independently
- runs OCR on each line
- joins the output in reading order

So multiline support in this repository is line-segmentation-based, not a full layout-transformer pipeline.

## Debug Outputs Returned By Prediction

When `--debug` or backend debug mode is enabled, the output can include:

- per-line bounding boxes
- text breakdown
- token trace with token IDs and approximate confidences
- character trace with Unicode codepoints
- preprocessing metadata
- line padding metadata

This is especially useful for understanding failures instead of only seeing a final string.

## Suggested Documentation Wording For Your Final Report

If you want polished project-report wording, the following is accurate for this repository:

> The OCR model is a fine-tuned TrOCR small printed checkpoint adapted for Brahmi script by expanding the tokenizer with Brahmi Unicode characters and training on a hybrid curated-plus-synthetic dataset. The dataset combines fixed class folders, mixed text images, and synthetic generation across clean, manuscript-like, and stone-like backgrounds in a 40/35/25 ratio. Runtime balancing was used to reduce character-only dominance and improve sequence learning for words, phrases, and multiline text.

And for the metrics section:

> In a documented validation run, the model improved from severe early sequence collapse in epoch 1 to a substantially more stable decoding regime by epoch 6, reaching a CER of 0.1969, a WER of 0.4051, an exact sequence match of 0.77, and a first-character accuracy of 0.96. For presentation, this can be described as roughly 80.3% character-level accuracy proxy on that validation run, while noting that it is not yet a formal benchmark.

## Limitations

- synthetic text is not the same as real epigraphic language distribution
- translation is a downstream convenience layer, not part of OCR accuracy
- no official held-out benchmark report is packaged here yet
- long-sequence OCR remains weaker than isolated character and short-word OCR
